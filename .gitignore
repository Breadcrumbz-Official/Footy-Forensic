# Python
__pycache__/
*.py[cod]
sfai_env/
.venv/
venv/

# Secrets — GEMINI_API_KEY lives here for a local run, never commit it.
server/.env
.env

# Node / Vite client
ui/node_modules/
ui/dist/

# NOTE: server/models/ is deliberately committed (pre-cached, ~54MB) so a fresh
# machine needs no internet on first run. models_cache.py downloads it if absent.

# OS
.DS_Store
Thumbs.db

# YOLO11x weights (114MB) — fetched by models_cache on first run, not committed.
server/models/yolo11x.pt
